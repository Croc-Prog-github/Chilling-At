{
  "name": "starter",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "publish": "vite --host",
    "start": "npm install && npx run vite",
    "postinstall": "chmod +x node_modules/.bin/vite"
  },
  "dependencies": {
    "vite": "^6.2.6"
  }
}