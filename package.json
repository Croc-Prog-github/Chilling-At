{
  "name": "starter",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "remove": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\" && cmd /c if exist dist rmdir /s /q dist",
    "preview": "vite preview",
    "start": "vite"
  },
  "devDependencies": {
    "vite": "6.3.5"
  },
  "engines": {
    "node": "22.x"
  }
}
